apiVersion: v1
kind: Service
metadata:
  name: droneapiworker
  labels:
    app: droneapiworker
    tier: backend
spec:
  ports:
  - port: 1236
    targetPort: droneapiworker
    name: droneapiworker
  type: NodePort
  selector:
    app: droneapiworker
    tier: backend